# JoePDF — example environment configuration
# Copy this file to .env.local and adjust as needed.
# .env.local is gitignored — never commit it.

# Optional: override the Vite dev server port
# VITE_PORT=5173
