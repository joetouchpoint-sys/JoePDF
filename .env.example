# JoePDF — example environment configuration
# Copy this file to .env and adjust as needed.
# All processing is browser-side; no secrets are required for the static build.

# Optional: override the Vite dev server port
# VITE_PORT=5173

# Optional: set a base path if deploying to a sub-directory
# VITE_BASE=/joepdf/
